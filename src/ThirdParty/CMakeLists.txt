# Step into the subdirectories
MESSAGE(STATUS "Stepping into OpenCV")
add_subdirectory(OpenCV)
MESSAGE(STATUS "Stepping into RmlUI")
add_subdirectory(RmlUI)
MESSAGE(STATUS "Stepping into SDL")
add_subdirectory(SDL)