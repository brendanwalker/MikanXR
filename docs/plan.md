# Plan

The living plan: what is in flight now, what comes next, and the open questions. Completed items are removed rather than checked off. Resolved questions are removed. Deferred work enters here at the moment of deferral.

## Now
- [ ] Node editor snapshot history commits a step on nearly every mouse release while the compositor runs, because a node's live `float_defaults/time` value serializes into the graph snapshot and the commit dedup sees a changed graph. A plain click on empty canvas after an undo adds a step. Keep runtime-fed values out of the snapshot, or dedup on the persisted fields only.
- [ ] `ShapeSelectNode` gathers every quad, box and model shape in the project rather than only those in the compositor's own scene, so a shape belonging to one scene draws into another scene's composite.
- [ ] Cursor feedback in the interactive browser window: `CefDisplayHandler::OnCursorChange` fires but nothing consumes it, so the pointer stays an arrow over links and text fields. Deferred because it needs a new `IMkWindowContext::setCursor` plus an SDL system-cursor cache, and because ImGui's SDL2 backend calls `SDL_SetCursor` from `ImGui_ImplSDL2_UpdateMouseCursor` every `NewFrame` and would immediately overwrite it. Doing it means toggling `ImGuiConfigFlags_NoMouseCursorChange` on that window's io while the pointer is over the page area.

## Next
- [ ] Localize the USB video setting slider labels. `GuiPanel_USBVideoSourceComponent` builds them at runtime from `k_videoSettingPropertyPrefixes` ("brightness" to "Brightness") inside a custom renderer, so they sit outside the descriptor label mechanism and still read English in every language.
- [ ] `VideoFrameDistortionView` only re-reads its source's intrinsics when the frame size changes (`ensureFrameBufferSize`), so swapping a USB source onto a camera with the same resolution leaves the view undistorting with the previous camera's coefficients until the stage is re-entered.
- [ ] `AppStage_AlignCameraByUtilityMarker` never applies the camera intrinsics to the viewport camera. Its overlay is drawn through whatever projection the viewport camera happens to hold.
- [ ] `AMikanLightEnvironmentActor::UpdateKeyLight` sets the key light with `SetWorldRotation` and feeds the skydome its SH parameters globally, but both are in the capturing stage's space now. Correct while no stage carries a rotation. A rotated stage needs the UE side to rotate them by the stage actor's transform first.
- [ ] `GuiPanel_ShapeComponent`'s parent picker offers scenes, anchors and shapes but not stencils, so a shape cannot be parented to a stencil from the panel even though the hierarchy allows it. The stencil and anchor pickers have the matching gap for shapes.
- [ ] `computeOpenCVCameraExtrinsicMatrix` (`src/Editor/Math/CameraMath.cpp`) has no callers. Delete it, or find the flow that was meant to use it.
- [ ] While a node editor window is open on a compositor, that window's in-memory graph shadows the file: reloading the compositor logs `CompositorComponent::handleCompositorNodeGraphChanged - Loaded compositor graph` and renders the stale graph anyway, so an edit made to the `.compgraph` on disk has no effect. Closing the window makes the identical edit apply immediately. The window owning the graph is reasonable; the silent divergence between a log line claiming a load and a render that ignores it is not. Either make the reload path take the file over the open window, or refuse the reload and say the window owns the graph.
- [ ] A live DMX change reaches the Unreal plugin's `UMikanDMXDataStore` but never refreshes the fixture actors, so a light keeps whatever colour it had when it bound. Reproduced on a cold Unreal process: setting `red` to 255 in the editor moves the store to channel 1 = 255 while the actor's `GetLightColor` stays on the previous value. The break is somewhere in `UMikanEngineSubsystem::OnDMXDataChanged` to `AMikanClient::HandleDMXDataChanged` to the per-actor `OnDMXDataChanged`. Masked until now because nothing ever lit up at all.
- [ ] Ten `Mikan*SystemValues` structs exist whose systems never override `getClientAPIValuesStructType`, so `SystemGetValuesRequest` answers the base `MikanSystemValues` for them: anchor, box/model/quad stencil, DMX fixture group, DMX preset, DMX sequence, light environment, RGB pixel grid, RGB spot light. Harmless today because all ten are field-less, but the first field added to any of them reads back as garbage on the client with nothing failing. Either add the overrides, or have the schema guard assert the accessor as well as the descriptors.
- [ ] `MikanARKitVideoSourceValues::k_ownerSystemName` ("ARKitVideoSourceSystem") and `MikanNetworkVideoSourceValues::k_ownerSystemName` ("NetworkVideoSourceSystem") do not match their systems' `k_objectSystemClassName` ("ARKitVideoObjectSystem", "NetworkVideoObjectSystem"), so a client listing components by the constant (`TestMikanClient.cpp`) finds nothing. Fixing either side is a bindings regeneration.
- [ ] `FileVideoSourceComponent` keeps the previous media's duration and a `playing` state when a new `media_path` fails to resolve, and logs nothing. Seen when a take was deleted under a component: the stage showed `Playing (0.00 / 6.37 s)` for a path that no longer existed. The open failure should surface in the log and the status.
- [ ] `ARKitVideoSourceComponent::notifyFrameBundleReceived` overwrites fx/fy/cx/cy but never recomputes `hfov`/`vfov`, so the camera frustum the editor draws for a phone comes from the default field of view. `createMonoIntrinsicsFromPinhole` (used by the file source) does both and the ARKit path should adopt it.
- [ ] Delete `tools/export_marigold_onnx.py` and repoint scene-lighting.md at MikanXR/MikanMarigoldOnnx, which owns the export now. Once that repo has published its first release.
- [ ] Python tooling split. Delete `tools/fetch_moge2_onnx.py` (the MoGe-2 fetch becomes a plain download, no `huggingface_hub`), move the torch-dependent scripts into their own subfolder, add a light `requirements.txt` and a pinned ML one, an install batch file at the repo root, and a table in commands.md saying which scripts need which.
- [ ] Byte-range resume for a model download. A failed or cancelled transfer restarts the file it was on, which for the 1.75GB Marigold parts is a real cost; `HttpDownloader` would need a Range request and an append-mode `.part`. File-level resume already exists, so only the file in flight is lost.
- [ ] Port `LocalizationRemoteFetcher` onto `HttpDownloader` so there is one WinHTTP implementation rather than two. The fetcher predates it and buffers whole responses into a string, which is fine for its kilobyte JSON but means the retry, redirect and truncation handling live in only one of the two.
- [ ] The automation `component create <system> <class>` verb calls `addNewObjectByTypedDefinition` with no definition-init callback, so it builds components in states the outliner's add actions never produce: an anchor or shape with no `parent_transform_id`, a camera or light with no owner stage. `ProjectOutlinerActions` sets those at creation and is the only path a person can take. A drive that creates objects this way is therefore testing a shape of object the editor does not otherwise have, which is how the scene-less gizmo crash was found. Either give the verb an optional parent argument it passes through, or say plainly in automation.md that it is a raw constructor and a drive that wants a realistic object sets the ids itself afterwards.


## Later
- [ ] Host the gettext catalogs on a translation platform, so a volunteer gets a web editor that opens its own pull request instead of installing POEdit and cloning. Runbooks are written for both candidates: [weblate-integration.md](./proposals/weblate-integration.md) (the recommendation, free Libre plan, gettext-native, fuzzy maps straight onto its "Needs editing" state) and [crowdin-integration.md](./proposals/crowdin-integration.md) (better editor, but the fuzzy round trip is unproven and the free tier requires contributing translations to its global memory). Either one first needs the same two repo changes: `tools/localization.py` comparing parsed catalogs rather than rendered bytes, so the platform's PO formatter and ours stop fighting, and a workflow that regenerates the JSON tables on the translation branch so the incoming pull request is not red. Worth doing when someone actually asks to translate, or sooner if a link to point recruits at is wanted.
- [ ] Material graph: per-node preview thumbnails, compiling a variant of the graph with the node's output as the color.
- [ ] Material graph: draw nodes auto-feeding well-known semantics such as time and screen size, so those parameters need no outer-graph pins. Today a `time` parameter is fed from the outer graph's `TimeNode` like the hand-written materials.
- [ ] Material graph: a parameter the author never wants exposed as a consumer pin. Every parameter is a pin today; a `bExposed` flag on the parameter nodes that keeps the uniform out of the consumer's pin list (bound only from its default) is the cheap follow-up.
- [ ] Shapes without a graph: a shape carrying only a material reference could draw through the material's defaults, the one-click path for materials whose inputs are all baked. `DrawShapesNode` already has a no-graph fallback with an internal material to grow from.
- [ ] Material graph: further shader writers (Vulkan GLSL dialect, HLSL, MSL) behind `IShaderWriter`.
- [ ] Material graph: shared function assets. A `MaterialFunctionPage` saved to its own `.matgraph` and referenced by path from several materials, as a second function source behind the existing call node and `callMaterialFunction`; the Functions panel would import one and the Assets panel would list the type. Worth doing once a second material wants a function another already defines.
- [ ] Material graph: multiple function outputs, which means `out` parameters on the writer seam beside the single return value.
- [ ] An fp16 Marigold export would halve the 3.5GB model download and drop every asset under GitHub's 2 GiB cap, so nothing would need splitting. Gated on re-validating accuracy against the reference: scene-lighting.md records fp32 as deliberate because fp16 constant folding loses precision.
- [ ] Node graphs: prune `assetReferences` entries no property binds to on save. Nothing shows the per-graph list any more, so a reference whose material or texture property was deleted stays in the file unused. Harmless until a graph accumulates many.
- [ ] Node editors: named reroutes for the compositor and shape graphs. The material graph has them (`ShaderRerouteDeclarationNode`, `ShaderRerouteUsageNode`); the evaluated graphs would need a usage node that copies a typed pin value per pin class at evaluation time.
- [ ] Material graph: more than one material editor window at a time. One instance today, matching the compositor and shape editors.
- [ ] Vulkan shared texture writer hardening, gated on measuring the cost against a real client rather than the cube harness. The first pass blits the client's image into a linked D3D11 texture, waits the fence, then sends through Spout's `SendTexture` copy, so a frame costs one CPU stall and one extra GPU copy. Vulkan is the only backend that needs either: DX11 copies on the client's own device, and DX12 hands its command queue to the D3D11On12 device so the copy is queue-ordered behind the client's rendering with no stall at all. There is no Vulkan equivalent of that trick, since D3D11 cannot submit to a `VkQueue`.
	- The stall and the copy are a fork rather than two steps, because Spout's `CreateSharedDX11Texture` deliberately uses `MISC_SHARED` with a named mutex for every texture (its comment cites DX9 receiver compatibility for refusing `SHARED_KEYEDMUTEX`). A texture arbitrary receivers open therefore cannot be keyed-mutex.
		- Keyed mutex (`VK_KHR_win32_keyed_mutex`) on the private linked texture removes the CPU stall and keeps the copy. It forecloses zero-copy, since the texture stays private, and the writer must create it rather than borrowing `spoutdx.CreateSharedDX11Texture`.
		- Zero-copy sender registration through `sendernames.CreateSender` over the linked texture's own handle removes the copy and keeps the stall, since Spout's named mutex is CPU-level and says nothing about whether the Vulkan blit landed. The writer takes over the sender bookkeeping `CheckSender` does today.
		- A shared semaphore (`VK_KHR_external_semaphore_win32` imported as an `ID3D11Fence`) lets the D3D11 copy wait on the GPU without touching the texture's flags, so unlike keyed mutex it composes with zero-copy. Closest analogue to what the DX12 path gets for free.
	- Client cost differs per option, which is why link-engine comes first: zero-copy is entirely writer-side, while keyed mutex and the shared semaphore each add a device extension the client enables at `vkCreateDevice`.
	- Deferring the fence wait to the start of the next write pipelines the stall behind the client's own frame with no new extension, at one frame of added latency. Not obviously worth it for mixed-reality compositing, so it is not a free fallback.
	- RGBA16F and the shadow buffer are mapped but unverified, since the test harness requests neither.
- [ ] link-engine as a Vulkan client: the engine side of the seam the Vulkan writer proves. The engine creates its device with `VK_KHR_external_memory_win32`, hands `MikanVulkanDeviceInterface` to the client API, and each frame hands the offscreen main view image with its layout as a `MikanVulkanTexture`, which means `render.hpp` needs an export of that image without leaking a Vulkan type. Depth arrives as an `R32_SFLOAT` color image, so the engine also needs a pass writing linear depth to one.
- [ ] Single-undo object bursts: outliner cascade delete and script trigger runs. Deleting a subtree today records one transaction per object, child-first, which undoes correctly in reverse but takes N undo steps, and a script trigger such as `GenerateLights` that removes and creates many objects lands the same way even though the run is bracketed as a gesture. Merging them needs `TransactionHistory` to append create and destroy ops into an open gesture instead of sealing on each (`m_pendingDestroyOp` is a single op today); verify with the `history` automation commands before keeping it.
- [ ] HTTP route editing over the automation channel: `script routes` lists the project's route table, but adding, retargeting, and removing a route still needs the HTTP Triggers panel. `property set ScriptObjectSystem <id> http_routes <json>` reaches it through the hidden property, which is enough for a drive script but not a verb.
- [ ] Sequence handler name migration: a project saved before sequences named a script component carries `sequence_name` strings that load unassigned, since a handler name maps to no component. Worth a one-time mapping only if a project with many sequences turns up; the panel re-pick covers the few that exist.
- [ ] Scene lighting: decide whether a single global probe suffices or region-of-interest probes near the character are needed. `LightEnvironmentComponent` already carries a world position, so this needs no wire-format change. Answerable only from the end-to-end look in Unreal.
- [ ] Scene lighting: calibrate the exposure scalar automatically instead of by hand per shoot.
- [ ] Depth proxy mesh: refine residual metric scale against tracked anchors or the floor plane, so a capture needs no ArUco marker in frame.
- [ ] Depth proxy mesh: occlusion-grade silhouette edges. The current proxy is a shadow catcher; it cannot occlude a character walking behind real furniture.
- [ ] Stereo calibration: `MikanStereoIntrinsics` and the rectification math exist but no stereo calibration AppStage does.
- [ ] Video recording: `eSupportedCodec` in `CompositorConstants.h` is defined but nothing records the composited output to a file.
- [ ] File video source hardware decode: `MovieDecoder` decodes on the CPU through OpenCV's ffmpeg backend. `cv::CAP_PROP_HW_ACCELERATION` can ask that backend for D3D11VA or CUDA decode with no other code change, worth measuring on a 4K phone recording before the software decode becomes the editor's tick budget.
- [ ] Recorded takes with distortion: the editor records undistorted frames so the pinhole sidecar is exact. Recording the raw frames instead would keep the take identical to the live camera, and needs an optional distortion coefficient array in the pose track (a format version bump) that `FileVideoSourceComponent` applies on playback.
- [ ] Asset upload memory: `ix::HttpServer` buffers a request body in memory (a vector, then the request string) before the route sees it, so an upload transiently costs two to three times its size in RAM. Streaming the body to disk needs a change inside the IXWebSocket fork.
- [ ] Asset listing for the phone: a `GET /assets/list?folder=<folderId>` beside the upload route would let the Captures panel mark takes already on the PC instead of tracking uploads per session.
- [ ] File video source playback rate: the clock advances at real time only. A `playback_speed` property would cover slow-motion review and frame stepping, and the scrub slider plus play and pause cover the rest today.
- [ ] Refcount module users in `MikanModuleManager`. `disposeModule` unloads the DLL on the first call, so a second holder of the same cached module is left with objects allocated inside an unloaded DLL. Every module has one owner in the app, so this only bites tests today: `arkit_video_source_system_test_independent_instances` leaks both its loaders to avoid it.
- [ ] Remove the empty `src/Libraries/ARKitReceiver` and `src/Libraries/MikanARKitReceiver` scaffolding directories.
- [ ] Stop checking generated bindings into git (`bindings/csharp/CMakeLists.txt` carries the TODO).
- [ ] Wire the unrecordable property notifications into descriptors so they reach undo and client events: the `EditorObjectSystem` settings names and `depth_mesh_scale_correction` (the standing gaps listed in `docs/reference/transactions.md`).
- [ ] macOS and Linux port, sequencing. The rendering API is not the blocker it looks like: macOS deprecated OpenGL in 10.14 but still ships a working 4.1 core profile, and MikanXR's usage sits well under that cap. Every shader is `#version 330 core`, `SdlWindowContextManager` requests a 3.2 core context, and the only call past GL 3.3 anywhere in `src/Libraries` or `src/Editor` is one debug-only `glDebugMessageCallback`. So the existing renderer runs on macOS as-is, and the port should be proven on GL 3.3 before any backend swap is considered. Start with the headless Linux compositor, since `MikanCmd` plus `-runTests` skips CEF, ImGui, OpenVR, and the window system entirely and isolates the dependency problem below.
- [ ] Crash minidump contents: `MiniDumpWithDataSegs` is left out to keep the file attachable, and an editor dump at the main menu with libcef loaded measures 2 MB without it. Measure what data segments add before restoring them; the win is global variables being readable in the debugger.
- [ ] Cross-platform crash reports. `CrashHandler.cpp` is Windows only (the SEH filter, the CRT hooks, and the minidump writer), with an empty stub elsewhere. The port needs its POSIX branch: signal handlers writing a backtrace from a forked child, or Breakpad-format minidumps if the report is to stay one file format across platforms. Sequence it with the headless Linux compositor, since `MikanCmd -crash` is how the branch gets verified.
- [ ] Cross-platform dependency acquisition. Everything under `deps/` is a Windows binary package fetched by `InitialSetup_x64.bat`, so each needs a per-platform path. RefurekuGenerator is the hard gate: four targets run it as a pre-build step and it ships as `rfk_v2.2.1_windows.zip`, so nothing builds without it. The rest in rough order of cost: CEF (works on macOS but needs the helper-process app bundle structure), `MikanOnnx` execution provider selection (DirectML-first today, needs CoreML on macOS and CUDA or CPU on Linux), `MikanWMFVideo` (needs AVFoundation and V4L2 siblings, which the plugin module contract already accommodates), OpenVR (Valve dropped macOS, so `MikanSteamVR` is Windows and Linux only), and the Winsock leaves in `MikanDMX/UdpMulticastSocket`, `AutomationSocket`, and `LuaDebugSocket`.
- [ ] Cross-platform shared texture transport. This is what makes a port worth having, and it is Windows-only end to end: `MikanSharedTexture` is Spout2 over DirectX shared NT handles, all four writers funnel through a D3D11 texture, and `SharedTextureReader` receives into a GL texture id. macOS needs an IOSurface path and Linux needs dma-buf or opaque FD. `SharedClientGraphicsApi::Metal` already exists in the enum with nothing behind it, so the accessor seam is in the right place. Protocol-touching, so it carries the wire-protocol rules and a bindings regeneration. Independent of any renderer decision.
- [ ] `MikanRenderer` immediate-state to pipeline-state refactor. `Public/` is already free of GL types and `createMkGraphicsContext` already takes an `eGraphicsAPI` listing Vulkan and Metal, but the semantics are a GL state machine: `DrawLayerNode` mutates blend function, blend equation, and stencil op inside the draw loop against a shared state stack. Every modern API bakes that state into an immutable pipeline object, so the `mkStateSet*` calls have to become a pipeline descriptor built before the pass, and `CompositorNodeGraph` has to reorganize around explicit render passes. That refactor is a valid OpenGL design on its own, so it can land incrementally under the existing backend with the test suites verifying no functional change, which touches the 144 consumer files once instead of twice. It is the prerequisite for any backend swap, and Vulkan with MoltenVK is the front-runner if one happens, since this branch already vendored Vulkan-Headers and volk, compiles SPIR-V through `deps/dxc`, and has a working `-vk` client path. Note that the material files (`.compmat`, `.shapemat`) are a user-facing asset format (`MaterialAssetReference` exposes a `*.mat` file dialog), so any shader-language change past GLSL breaks projects that hand-author materials. Materials authored as graphs survive it: the material compiler emits through `IShaderWriter`, so the change is a second writer plus recompiling every `.graph` (materials.md). Custom expression nodes carry raw GLSL and would need rewriting by hand.

## Open questions